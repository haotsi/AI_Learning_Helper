# 更新记录

## v4.2 · 2026-09-14 新增第四学科板块「🧠 数理逻辑」（17 章教程 + 24 题 + 全链路接入）

**板块与内容**

- **新数据层 `assets/js/data/logic.js`**：`APP_DATA.logic` 共 **17 章**，与 ds / prob 完全同构（`{id, cat, title, readTime, points, content}`），按五大阶段分组：
  - 逻辑基础（l01 数理逻辑是什么 / l02 命题与真值 / l03 逻辑联结词 / l04 真值表）
  - 命题逻辑（l05 逻辑等价与等价律 / l06 原命题·逆命题·否命题·逆否命题 / l07 范式 DNF 与 CNF / l08 推理与推理规则）
  - 谓词逻辑（l09 谓词与个体 / l10 全称量词与存在量词 / l11 量词的否定 / l12 量词的顺序与多重量词）
  - 证明方法（l13 直接证明与分类讨论 / l14 反证法与逆否证明 / l15 存在性证明与反例 / l16 数学归纳法）
  - 计算机逻辑（l17 数理逻辑与计算机科学，含极简 Lean 示例并说明它就是 Modus Ponens）
- 每章统一按「概念 → 直觉解释 → 数学定义 → 示例 → 常见错误 → 本章小结」组织，**先直觉再形式化**；真值表、等价律、量词对照等均用现有 `<table>` 样式，公式全部走已内置的 KaTeX（零新依赖）
- **学习路径复用既有数据结构**：五个阶段即章节 `cat`，由 `pages/logic.js` 从数据推导渲染成 `#logicRoadmap` 阶段链（复用 `.mainline` / `.flow-node`，阶段名不硬编码），不另起第二套路径系统；AI 专属的 `APP_DATA.stages` 未改动

**接入面（首页 / 导航 / 知识库 / 测验 / 搜索 / AI 助手）**

- 首页新增第四张主题卡（宝蓝 `card-logic`，简介 + 「进入板块」），学科区标题改为「四大学科板块」；Hero 主标题改「四门核心能力」；`#statSubjects` 自动变 4（原本就由 `Object.keys(APP_META.subjects).length` 计算）
- 导航在「概率统计」后新增「数理逻辑」；新路由 `#/logic`、`#/logic/<章节id>`（`router.js` 的 `VIEWS` 增至 8 项），布局与 ds / prob 页一致（左目录 + 右正文 + 章末上一条/下一条/配套练习/跨学科延伸）
- **测验**：`logic-quiz.js` 追加 **24 道**逻辑题（全站 61–84 题，入门 8 / 进阶 10 / 挑战 6），15 个 `cat` 全部登记学科归属与知识点直达路由；每题 4 选项 + 答案 + 解析 + **3 条错误选项「为什么不选」**
- **学科筛选去硬编码**：测验学科 chips、结果页能力分析行、知识库学科筛选与分组顺序全部改为遍历 `Object.keys(APP_META.subjects)`；`#/quiz/sub-<学科>` 的学科名改为校验存在性而非枚举；页脚学科串改为数据拼接。以后再加第五科无需改这些逻辑
- **搜索**：`utils/search.js` 新增一行逻辑章节索引（沿用同一引擎），「数理逻辑 / 真值表 / 蕴含 / 德摩根律 / CNF / 谓词 / 全称量词 / 数学归纳法」等关键词可搜到并跳转对应章节；搜索框示例词补「真值表」「德摩根律」
- **AI 助手**：`chat-rules.js` 追加 6 条数理逻辑问答规则（板块总览 / 命题·联结词·真值表 / 蕴含 `P→Q` / 四种命题 / 量词与量词否定 / 证明·推理·范式·计算机），快捷问题新增 4 个（什么是命题、∀ 与 ∃ 的区别、`P→Q` 为什么这样定义、怎么否定量词命题）；「继续学习」按钮的路由白名单加入 `logic`
- 主题层补齐第四学科：`--color-logic #2563eb`（+ light/deep）、`.badge.subj-logic`、`.subject-card.card-logic`、`.bd-fill.logic`、`.page-head.accent-logic`、`body[data-page="logic"] .kb-article h2` 左边线；`icons.js` 新增 `brain` 描边图标；`utils/math.js` 的 `SYM` 补 `\vdash \therefore \because \models \top \bot \oplus` 等逻辑符号（供搜索片段与 KaTeX 缺失降级）

**响应式（第 4 个导航项 / 第 4 张卡的连带处理）**

- 新增 `.grid-4`：≥1240px 一行四张、平板 2×2（避免 3+1 孤卡）、≤620px 单列；覆写规则置于 `responsive.css` 的 900px 块之后（同特指度靠源码顺序取胜，否则被 `.grid` 的 `auto-fit` 压回 3+1）
- 导航逐级压缩内边距与字号（≤1220 / ≤1060 两档）+ 721–960px 横向拖动兜底，未提高 720px 汉堡折叠阈值、未改 `--header-h`

**测试与文档**

- `scripts/check.mjs` 断言 89 → **136**（全绿）：新增 ④b 段（逻辑板块直达 / 17 章目录 / 五阶段路线 / 分组 / KaTeX 与表格 / 无残留分隔符与标红 / 学科主题色逐项落到 UI / brain 图标非兜底 / 下一条跳转）、知识库 logic 筛选 17 章、测验学科筛选 24 题与逻辑题判分与逐项注释、首页第四卡与页脚与导航文案、⑦ 段 12 个逻辑关键词搜索命中 + 结果直达章节、⑬ 段多视口导航溢出（1440/1280/1200/1100/1000/900/800/760）与四卡列数（4/4/2/2/1）与 375px 逻辑页公式渲染；⑫ 段窄屏表格检查扩为 p06 + **l03（六列真值表，全站最宽）** + l05（等价律大表）并新增「正文子元素不得溢出正文盒」；同步原硬编码计数（首屏 4/12/84、收录 69 篇、第 x/84、四学科能力分析、file:// 四板块、375px 抽查路由）
- README（目录树 / 路由表 / 验证清单 / 扩充内容）、agent.md（路由表 / 首页 / 学习路径 / 知识库 / 教程页 / 测验 / 样式 / 数据约定，含「题库扁平数组 + 题号偏移」与「`\n` 开头命令必须双写反斜杠」两条新警示）同步更新
- `sw.js`：`ASSETS` 补三个新文件，`CACHE` 版本号 `v6 → v7`；`manifest.webmanifest` 的 `description` 同步为四大板块

## v4.1.1 · 2026-09-14 全项目排查与修复（移动端溢出 / 公式降级 / 缓存健壮性）

**移动端横向溢出（本次最高优先级；排查前 375px 视口下 8 个路由溢出 102–263px）**

- **根因**：`responsive.css` 在 ≤900px 把 `.kb-layout` 塌成 `grid-template-columns: 1fr`，而 grid 子项默认 `min-width: auto`，于是被 230px 的横向章节条（`.kb-item`）与 575px 宽的 5 列表格撑破整页。宽表格与 5 列布局是既有内容结构，与公式改造无关；KaTeX 行内 `nowrap` 让表格最小宽度进一步变大
- **修复（最终方案：按要求取消横向滚动，改为自适应换行）**：
  - 塌缩列改用 `minmax(0, 1fr)`，并给 `.kb-layout > *` 加 `min-width: 0`，消除 grid 子项 `min-width:auto` 撑破整页
  - 窄屏表格改 `table-layout: fixed` + 字号 12.5px + 收紧内边距，单元格 `overflow-wrap: anywhere` 换行
  - 行内 `.katex` 去掉 `white-space: nowrap` 与 `inline-block + overflow-x`，过长公式按断行点折行（实测 375px 折行 6 处、320px 11 处；KaTeX 0.18 无不可断行的 `.base` 包裹，可正常折行）
  - p06「超几何」行的 `\dfrac` + `\left(...\right)`（KaTeX 视为不可断行单元）改为 `\frac` + 普通括号，消除该单元格 42px 内部溢出
- **保留的横向滚动（均为原始设计，未动）**：独立公式卡片 `.katex-display`（长公式专用通道）、代码块 `pre`、手机端章节/笔记条 `.kb-list`
- **结果**：375px 与 320px 下 13 个路由页面溢出均为 0，表格与单元格内部溢出 ≤1px（修复前 p06 溢出 245px、ds14 263px、p12 162px）

**数学公式（KaTeX 层）**

- **降级路径正则写错**：`App.math` 的 `degrade()` 行内公式闭合分隔符误写为 `\\\]`，导致 KaTeX 整体不可用时行内 `\(...\)` 无法降级、页面残留生记号；已修正为 `\\\)`
- **`plain()` 纯文本转换丢字符**：`\mid` / `\lt` / `\gt` / `\|` / `\{` / `\%` 等未登记，搜索片段与降级文本出现 `P(A B)`、`X 3`、`S=\1,2,3\`、`68\%` 之类的残渣；补齐符号表并新增通用转义还原，全站搜索索引文本现为 0 反斜杠
- **无障碍回退**：移除 `output:"html"`，恢复 KaTeX 默认 `htmlAndMathml`，屏幕阅读器可读到 MathML（实测 79 个公式节点带 `<math>`）

**Service Worker**

- `install` 由 `cache.addAll(ASSETS)` 改为逐个 `cache.add` 并吞掉单个失败：此前 ASSETS 中任何一个资源缺失都会让新 SW 安装失败，用户会永久停留在旧缓存版本
- 修正文件头注释：sw.js 并不在 ASSETS 列表内，其更新取决于浏览器脚本更新检查与 GitHub Pages 的 HTTP 缓存（`max-age=600`）

**测试与文档**

- `scripts/check.mjs` 断言 80 → **89**：新增「概率章节含公式速查表」「表格内公式渲染 + 代码块完全隔离」「重复 typeset 幂等」「公式含 MathML」「KaTeX 缺失降级」，以及 ⑫ 段「375px 逐路由无横向溢出」「知识库无横向溢出」「窄屏表格自适应换行（表格与单元格无内部溢出）」「无表格横向滚动容器」
- README 验证清单补移动端无溢出 + 自适应换行项与断言数；agent.md 补 ⑫ 段说明与三条硬约束（`minmax(0,1fr)`、行内公式禁止 nowrap/inline-block 滚动、表格内禁用 `\left(...\right)` 大分式）

## v4.1 · 2026-09-09 KaTeX 数学公式渲染（概率统计板块 679 条公式全面 LaTeX 化）

**公式方案**

- **本地内置 KaTeX**（`assets/vendor/katex/`：katex.min.css/js + auto-render + 仅保留 woff2 字体的 20 个字体文件，约 546 KB）：零 CDN、零构建、零后端，`file://` 双击直开与 GitHub Pages 子路径部署照常；新增/改名文件已同步 `sw.js` 预缓存列表并把 `CACHE` 版本号升到 `v5`
- **统一记法**：行内公式 `\( ... \)`、独立公式 `\[ ... \]`（数据 JS 字符串中反斜杠双写 `\\(`）；`prob.js` 概率统计 14 章正文、`points` 核心知识点与全部「公式速查」表格内的分数、上下标、根号、求和、积分、期望方差、分布记号、假设检验符号全部改为 LaTeX（共 679 条），旧式 Unicode 假下标（x₁、ᵢ）、`<sup>/<sub>` 拼分式与 `<code>` 包裹公式清零
- **动态渲染**：`App.math.typeset()` 封装 auto-render（忽略 `pre/code` 与已渲染 `.katex`，幂等可重复调用），在路由切换（router.js）、测验重渲染（quiz.js）、弹窗打开（modal.js）、聊天消息（chat.js）四处自动触发
- **失败降级**：单条公式解析错误 KaTeX 以 `throwOnError:false` 标红原文、不影响整页；KaTeX 整体加载异常时自动把 `\( \)` 剥成可读纯文本（σ²、x_i、1/2 风格映射）并 console.warn 提示；新增 `assets/css/math.css` 统一公式卡片样式（浅色底、细边框、长公式横向滚动、移动端降级、主题令牌进 variables.css）
- **搜索兼容**：全站搜索索引对公式段做 LaTeX→纯文本转换，片段不出现 `\( \frac` 噪声
- **测试**：`scripts/check.mjs` ④ 段新增 4 条 KaTeX 断言（公式节点数 / 分数与上下标结构件 / 无未渲染 `\( \[` 残留 / 无错误标红），全量断言 76 → 80

## v4.0.1 · 2026-09-09 缓存升级与收尾清理

- **修复老用户可能停留在旧版页面**：`sw.js` 的 `CACHE` 版本号 `v3 → v4`。v4.0 拆分使资源列表从 10 项扩到 40+ 项，但版本号未同步递增；由于 `sw.js` 自身也在预缓存列表中，老用户的浏览器更新检查可能命中旧缓存而拿不到新版本，导致长期停留在拆分前的单文件页面。同时在文件头补注「版本号是唯一的升级手段」，避免后续结构级改动再漏。
- **删除 `index.backup.html`**（392 KB，占仓库内容体积 44%）：拆分为 v4.0 时该文件仅被记录为「确认稳定后可删」，现已核对其内联数据与 `assets/js/data/` 逐字节一致（`stages` / `articles` / `ds` / `prob` / `dsEx` / `quiz` 六个键 sha256 完全相同，且不含任何独有内容），v4.0 已上线，正式移除；README 目录树与安全提醒、`agent.md` 结构清单同步更新。
- **`manifest.webmanifest` 描述文案**：`零依赖单文件网站` → `零依赖纯静态多文件网站`，与拆分后架构及「添加到主屏幕」的系统提示一致。

## v4.0 · 2026-09-09 单文件拆分 + 学习体验全面升级

**工程结构**

- **单文件 → 模块化多文件**：`index.html`（392KB）拆为「视图骨架 + assets/css 5 层样式 + assets/js 数据/工具/状态/组件/页面/路由 6 层脚本」共 40+ 文件；采用**有序普通脚本**而非 ES Module，保留 `file://` 双击直开能力，GitHub Pages 子路径部署照常
- **内容语义零改动**：原 `APP_DATA` 的 stages / articles / ds / dsEx / prob / quiz 字面原样迁移到 `assets/js/data/`；新增 `meta.js`（学科归属、前置知识、学习成果、难度分级等引导元数据）与 `question-notes.js`（每题错误选项注释）作为扩展层
- **进度持久化**：新增 `assets/js/state.js`，学习进度（已完成课程/小节、答题记录、错题本、最近路由）统一存 localStorage；`sw.js` 升级 v3 并预缓存全部新资源；新增 `scripts/check.mjs` 零依赖无头冒烟测试（真实浏览器驱动，30+ 断言）

**产品与体验**

- **首页**：首屏降密度（主标题 / 说明 / 两个按钮 / 3 个统计）；新增「我的学习进度」卡（无记录时给出首课引导）；学科卡（主题色，学什么）与工具卡（线框，怎么学）视觉分层；统计精简为 3 科 · 12 课 · 60 题
- **学习路径**：每阶段显示预计总时长 / 完成数 / 当前推荐课程；课程卡新增前置知识、学完成果、小节进度与「开始学习 / 查看详情 / 关联练习」按钮；跨阶段主线推荐链（Python 速成 → 机器学习 → 深度学习 → RAG 与智能体）；弹窗内小节可勾选、可标记整课完成
- **知识库**：统一收录 AI 笔记 + 数据结构 + 概率统计共 52 篇，按学科 / 难度 / 标签三维筛选；站内搜索按「课程 / 知识笔记 / 练习题」分组；空结果友好提示；文章底部提供上一条 / 下一条 / 前置知识 / 关联课程 / 配套练习
- **练习测验**：连续铺开改为一题一答（可切每页 5 题），顶部「第 n / N 题」进度；即时批改附正确答案、逐项「为什么不选」解析与相关知识点直达；进度自动保存、可重开；答错自动进**错题本**，支持 ☆ 收藏与重做；结束页给出总分 / 正确率 / 三学科能力分析 / 薄弱知识点 / 推荐复习内容；题库结构预留 `type` 字段可扩展新题型
- **AI 助手**：明确产品边界「当前为站内知识问答，暂不支持开放域对话」；快捷问题按学习规划 / 概念解释 / 题目讲解 / 课程推荐四类组织；回复自动附「继续学习 / 查看练习题」操作；未命中时给出规范兜底文案；真实 API 扩展点保留于 `pages/chat.js` 的 `CONFIG`（不暴露密钥、无后端）
- **视觉**：全局改 CSS 变量驱动；主色渐变仅用于主按钮与重点数字；阴影收敛、圆角/间距统一；三学科主题色（AI 紫 / 数据结构蓝绿 / 概率统计橙）；界面 emoji 大规模替换为统一 SVG 描边图标；键盘可达（focus-visible、1-4/A-D 答题、跳到主要内容链接、弹窗焦点回归）；移动端首屏收紧

**文档**

- README 重写：目录结构、路由表、运行与部署、上线前逐项验证清单、内容扩充指南；原单文件保留为 `index.backup.html`

## v3.2 · 2026-09-04 PWA：一键安装为手机 App

- **可安装为 App**：新增 Web App Manifest（名称/图标/主题色/standalone 显示模式），手机「添加到主屏幕」后一键全屏打开，无浏览器地址栏
- **离线可用**：新增 Service Worker——预缓存核心资源、缓存优先回源回填、离线兜底返回整站页面；`sw.js` 的 `CACHE` 版本号 +1 即可让全部客户端刷新缓存
- **应用图标**：GDI+ 生成 192 / 512（含 maskable）/ apple-touch-icon 180 三档，靛蓝渐变 + "学"字主视觉
- 本地 file:// 双击打开自动跳过 Service Worker 注册，不影响既有用法；README 新增 iOS / Android / 桌面端安装步骤与 GitHub Pages 部署说明
- **主文件更名为 `index.html`**（原 `ai-learning.html`，git 保留重命名历史）并新增 `.nojekyll`：GitHub Pages 根路径不再渲染 README，直接打开应用

## v3.1 · 2026-09-04 内容丰富化、搜索与 AI 增强

**内容**

- **概率论 14 篇全面扩写**：每篇新增「经典例子」「经典公式速查 · 补遗」「直观理解」「常见误区」四类板块，全系列对标盛骤《概率论与数理统计》（浙大 · 第六版）；经典素材涵盖分赌本问题、蒲丰投针、女士品茶、高尔顿钉板、普鲁士骑兵与泊松分布、标记重捕、狼来了的贝叶斯演化、香农"二十个问题"等
- **数据结构新增三篇前置章**（全站 23 篇）：「C 语言程序设计」（面向有 C 基础的读者复习语法与常用库：指针/内存管理/qsort 与函数指针/stdio·stdlib·string·math·ctype 速查、printf·scanf 格式表）、「C++ 基础语法速览」（程序骨架、类型陷阱、控制流、引用传参、vector/string/pair、报错速查）＋「C++ 面向对象入门：类 · 继承 · 多态 · 友元」（访问控制与封装、构造/析构与 RAII、virtual 动态绑定与虚析构、纯虚函数抽象接口、friend 三铁律、运算符重载对接 sort/priority_queue、浅拷贝与对象切片等误区）

**功能**

- **全站搜索**：导航 🔍 / 快捷键 `/` 或 `Ctrl+K` 呼出；覆盖课程、知识库、数据结构、概率统计的标题与正文；加权评分排序、片段高亮、↑↓/Enter 键盘导航；中文整词未命中时按字模糊匹配（停用字过滤、字频降权近似 IDF、主题首字加权）；课程支持 `#/courses/课程id` 直达详情弹窗
- **AI 助手智能化**：关键词首中改为**意图评分**；问答库未命中时自动全文检索站内内容并附直达链接；回复中的站内路由与网址渲染为可点击链接；新增搜索用法、C++ 速览、刷题规划、智科外站等规则

**联动与外链**

- **力扣原题跳转**：25 道典型例题中 23 道力扣真题在卡片右上角新增「力扣 N 原题 ↗」直达 leetcode.cn；数据结构《刷题路线》表中的题号也全部可点击
- **GitHub 仓库外链**：导航栏 GitHub 图标 + 页脚「源码」链接，直达 github.com/haotsi/AI_Learning_Helper

## v3 · 2026-09-04 题库扩充与排序优化

- **题库 31 → 60 题**：新增 29 题，补齐数据结构（15 题）与概率统计（12 题）覆盖，另含深度学习与信息论题
- **「全部」栏目改为按题号顺序排列**：难度筛选只做显隐、不再打断答题状态
- C++ 在线判题经评估未纳入：纯前端单文件架构无法安全编译执行 C++（需自建后端判题机或加载约 30~80MB 的 WASM 编译器），不符合「双击即用」定位
- 同步更新 README

## v2 · 2026-09-04 主题改版与界面优化

- **主题更名**：「AI 学习助手」→「**学习助手**」；首页改为学科并列——AI 人工智能、数据结构与算法、概率论与数理统计三大板块平级呈现，AI 不再是唯一要素
- **布局优化**：主容器 1080px → 1360px，宽屏左右留白显著减少；卡片网格改 `auto-fit` 自适应铺满；知识库/数据结构/概率统计侧栏目录加宽至 320px
- **新增外站接口**：接入南京大学智科学生 Wiki「智科全家桶」（njuis-students.github.io），含顶部导航、首页「外站接口」区块（学习/科研/职场攻略直链）与页脚共三处入口
- 同步重写 README

## v1 · 初始版本

- 单文件整站（样式 + 数据 + 逻辑 + 视图）：hash 路由、学习路径 12 课、知识库 15 篇、数据结构 C++ 教程 20 章（附 25 道例题）、概率统计教程 14 篇、31 题分级测验、AI 答疑助手（模拟回复，预留真实 API）
